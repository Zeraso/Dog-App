<h1>You chose <%= @dog.name %></h1>
  <ul>
    <li>Name: <%= @dog.name %></li>
    <li>Birthday: <%= @dog.birthday %></li>
    <li>Gender: <%= @dog.gender %></li>
    <li>Breed <%= @dog.breed %></li>
    <li>Coat <%= @dog.coat %></li>
  </ul>

<% if policy(@dog).destroy? %>
<%= link_to "delete", @dog,
      method: :delete,
      data: { confirm: "Are you sure?" } %>
    <% end %>
<%= link_to "go back to homepage", dogs_path %>


